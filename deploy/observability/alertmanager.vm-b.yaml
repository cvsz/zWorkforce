# TEMPLATE / DOCUMENTATION ONLY.
# Production Compose mounts alertmanager.rendered.yml, generated by render-config.sh.
# Do not place receiver credentials or secret-bearing URLs in this tracked file.
route:
  receiver: operator
receivers:
  - name: operator
    webhook_configs:
      - url: "RENDER_REQUIRED"
        send_resolved: true
