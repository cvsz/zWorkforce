interface:
  display_name: "ZWorkforce"
  short_description: "Repo-specific patterns and workflows for zWorkforce"
  default_prompt: "Use the zWorkforce repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true