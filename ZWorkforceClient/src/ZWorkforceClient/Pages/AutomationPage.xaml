<local:PageBase
    x:Class="ZWorkforceClient.Pages.AutomationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ZWorkforceClient.Pages">
    <Grid Padding="{ThemeResource PagePaddingThickness}" RowSpacing="14">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <Grid ColumnSpacing="12"><StackPanel Spacing="4"><TextBlock Text="AUTOMATION" Style="{StaticResource CaptionTextBlockStyle}" /><TextBlock Text="Workflows and schedules" Style="{StaticResource TitleTextBlockStyle}" /></StackPanel><Button Grid.Column="1" Content="Refresh" Click="OnRefreshClick" /></Grid>
        <InfoBar Grid.Row="1" x:Name="AutomationInfo" IsOpen="False" IsClosable="False" />
        <TabView Grid.Row="2" x:Name="AutomationTabs" SelectionChanged="OnTabChanged">
            <TabViewItem Header="Workflows" Tag="workflows"><ListView x:Name="WorkflowsList" /></TabViewItem>
            <TabViewItem Header="Runs" Tag="workflow-runs"><ListView x:Name="RunsList" /></TabViewItem>
            <TabViewItem Header="Schedules" Tag="schedules"><ListView x:Name="SchedulesList" /></TabViewItem>
            <TabViewItem Header="Event rules" Tag="event-rules"><ListView x:Name="EventRulesList" /></TabViewItem>
        </TabView>
    </Grid>
</local:PageBase>
