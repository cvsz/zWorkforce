<local:PageBase
    x:Class="ZWorkforceClient.Pages.AgentsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ZWorkforceClient.Pages">
    <Grid Padding="{ThemeResource PagePaddingThickness}" RowSpacing="14">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <Grid ColumnSpacing="12"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel Spacing="4"><TextBlock Text="WORKFORCE" Style="{StaticResource CaptionTextBlockStyle}" /><TextBlock Text="Agents" Style="{StaticResource TitleTextBlockStyle}" /></StackPanel><Button Grid.Column="1" Content="Refresh" Click="OnRefreshClick" /></Grid>
        <InfoBar Grid.Row="1" x:Name="AgentsInfo" IsOpen="False" IsClosable="False" />
        <ListView Grid.Row="2" x:Name="AgentsList" AutomationProperties.Name="Agents">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8" ColumnSpacing="16"><Grid.ColumnDefinitions><ColumnDefinition Width="200" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{Binding Name}" FontWeight="SemiBold" /><TextBlock Text="{Binding Id}" FontSize="11" Opacity="0.65" /></StackPanel><TextBlock Grid.Column="1" Text="{Binding Description}" TextWrapping="Wrap" Opacity="0.8" /><TextBlock Grid.Column="2" Text="{Binding Status}" /></Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</local:PageBase>
