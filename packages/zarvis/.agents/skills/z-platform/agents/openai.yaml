interface:
  display_name: "Z Platform"
  short_description: "Repo-specific patterns and workflows for z-platform"
  default_prompt: "Use the z-platform repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true