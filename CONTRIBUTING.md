# Contributing
Create focused branches, run `make check`, add negative-path tests for security boundaries, use clear commit messages, and describe behavior/risk/rollback/validation in pull requests. Never commit secrets, production prompts, SQLite files or audit exports.
